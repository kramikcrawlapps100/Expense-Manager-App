<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="com.example.expensemanagerapp.permission.ACCESS_PROVIDER" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseManagerApp"
        tools:targetApi="31">

        <provider
            android:name=".MyContentProvider"
            android:authorities="com.example.expensemanagerapp.provider"
            android:exported="false" />

        <receiver android:name=".DatabaseChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.expensemanagerapp.ACTION_DATA_CHANGED"/>
            </intent-filter>
        </receiver>

        <activity
            android:name=".AnotherActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>